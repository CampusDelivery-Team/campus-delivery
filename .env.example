# Local example via SSH tunnel to the shared cloud Oracle instance.
# Real credentials are provided separately by the server maintainer.
ConnectionStrings__OracleDb=User Id=<database_user>;Password=<database_password>;Data Source=localhost:15210/orclpdb1;
