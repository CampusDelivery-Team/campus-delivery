# 总集成负责人说明

## 当前已完成

- 公共 Oracle 连接 Helper。
- 统一返回结果 `Result`、`Result<T>`。
- 中文常量 `SystemConstants`。
- 登录、退出、Session、角色跳转。
- 角色权限过滤器 `RoleAuthorizeAttribute`。
- 三类角色首页。
- 公共 Layout、顶部栏和角色菜单。
- 管理员节点管理示例，调用链为 Controller -> Service -> Repository -> OracleDbHelper。
- 其他模块占位 Controller 和 TODO 页面。

## 后续集成重点

- 密码验证从普通字符串比较改为哈希验证。
- 注册功能接入 `users` 表写入。
- 各业务模块按 SQL 文件字段继续实现。
- 每个模块提交前先 build，再让总集成负责人统一合并。
- 演示前准备 `admin / 123456 / 管理员` 测试账号和若干 `nodes` 测试数据。
