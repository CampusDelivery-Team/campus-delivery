@{
    ViewData["Title"] = ViewBag.ModuleName;
}

<h1 class="h4 mb-3">@ViewBag.ModuleName</h1>
<div class="card shadow-sm">
    <div class="card-body">
        <p class="mb-2"><strong>涉及表：</strong>@ViewBag.Tables</p>
        <p class="mb-0">@ViewBag.Todo</p>
    </div>
</div>
