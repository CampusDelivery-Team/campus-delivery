@model List<ReviewListItemViewModel>

@{
    ViewData["Title"] = "评价详情";
    int taskId = ViewBag.TaskId ?? 0;
}

<section class="panel management-panel">
    <div class="section-header management-heading">
        <div>
            <p class="eyebrow">用户反馈</p>
            <h1>任务 #@taskId 的评价</h1>
            <p class="section-description">查看该任务对应的服务评价。</p>
        </div>
        <a class="button primary" asp-action="Create" asp-route-taskId="@taskId">
            + 添加评价
        </a>
    </div>

    @if (TempData["SuccessMessage"] is string success)
    {
        <div class="inline-notice success">@success</div>
    }
    @if (TempData["ErrorMessage"] is string error)
    {
        <div class="inline-notice error">@error</div>
    }

    <partial name="_ReviewTable" model="Model" />
</section>
