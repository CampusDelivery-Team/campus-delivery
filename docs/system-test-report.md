# 校园中转分发与跑腿服务管理系统测试报告

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 1.2 |
| 测试对象 | `CampusDelivery.Api`（ASP.NET Core MVC + Oracle） |
| 测试人员 | 开发团队 |
| 自动化测试时间 | 2026-08-22 |
| 数据库系统测试时间 | 2026-08-22 完成共享库只读结构与查询核验；写入型端到端用例仍待执行 |
| 测试结论 | 39 项高价值业务自动化测试和静态门禁全部通过；共享库 24 张表及 `003` 至 `006` 迁移已验证，公开 HTTPS 入口和新版路由可访问；评价、投诉、审计及完整结算状态流转仍缺共享库写入型验收证据 |

## 2. 测试目的

本次测试不是以简单增删改查数量为目标，而是验证校园跑腿系统的完整业务链路、权限边界、事务一致性、状态机、重复提交和异常输入处理。重点证明以下能力：

1. 核心业务规则位于 Service 层，Controller 不直接访问数据库。
2. 抢单、支付等高风险写操作具有事务边界和行锁设计。
3. 任务、支付、退款等对象不能随意进行非法状态转换。
4. 越权、空字段、非法枚举、不存在 ID、重复提交等异常不会造成脏数据。
5. 测试结果区分“已自动执行”“静态检查通过”“待数据库执行”和“已知缺陷”，不把未执行用例写成通过。

## 3. 测试环境

| 类别 | 环境 |
| --- | --- |
| 操作系统 | Windows，PowerShell |
| 应用框架 | .NET SDK 9，ASP.NET Core MVC，目标框架 `net9.0` |
| 自动化测试 | xUnit 2.9.2、Microsoft.NET.Test.Sdk 17.12.0、VSTest 17.14.1 |
| 数据库设计目标 | Oracle 19c；结构脚本包含 24 张关系表 |
| 本次数据库连接 | 已通过 SSH 隧道连接共享 Oracle；只执行结构、权限、数据计数和应用查询等只读检查 |
| 浏览器 | 数据库手工测试时使用 Chrome/Edge 最新稳定版，两个独立配置文件模拟并发用户 |
| 配置方式 | `ConnectionStrings:OracleDb`，可由 `appsettings.Local.json` 或环境变量覆盖；真实凭据不入库 |

自动化和静态检查统一执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-tests.ps1
```

## 4. 当前测试能力与缺失分析

### 4.1 已具备能力

- 小型 `CampusDelivery.Tests` 测试工程覆盖 `AssignService`、`TaskService`、`PaymentService`、`ReviewService`、`AddressService`、`SettlementService` 和 `ReportService` 七个高价值业务服务。
- 使用仓储接口测试替身隔离 Oracle，能够稳定验证 Service 的规则、事务提交/回滚和模拟行锁下的并发结果。
- 自动检查 24 张表、全部 MVC POST 防伪令牌、五层边界、仓储层 `FOR UPDATE` 以及连接字符串外置。
- 代码中抢单先锁定任务行和跑腿员行，再在同一事务内更新任务、跑腿员、接派记录和状态日志。
- 已形成数据库端到端手工步骤、并发复核 SQL 和证据留存规范，见 `manual-system-test-guide.md`。

### 4.2 当前缺失

- 仓库已有自动化测试工程和本地质量脚本，但尚未配置持续集成工作流。
- 共享库迁移和只读查询已验证；为避免污染团队数据，本轮仍未执行评价、投诉、审计、并发抢单等写入型端到端用例。
- MVC 接口使用 Cookie 和 Anti-forgery Token，不适合直接提交一个带固定令牌的 Postman 集合；已提供可复现的 Postman/浏览器执行说明。
- 自动化并发测试验证的是 Service 在“事务行锁语义”下只能成功一次；真实 Oracle 的锁等待和最终表数据仍须执行 `TC029` 才能作为数据库级证明。
- 评价、报表、地址和结算的代码缺陷已通过 Service 自动化测试；唯一索引及评价查询已经共享库只读验证，但真实写事务和页面主链路仍须在隔离库执行，不能把 `PENDING-DB` 用例直接改为通过。

## 5. 测试范围与功能点

| 编号 | 模块 | 功能点 | 业务逻辑 |
| --- | --- | --- | --- |
| F01 | 用户 | 注册、密码哈希、账号和手机号唯一 | 是 |
| F02 | 用户 | 登录、退出、封禁/注销账号处理 | 是 |
| F03 | 权限 | USER、RUNNER、ADMIN 路由授权 | 是 |
| F04 | 地址 | 新增、编辑、删除、设置默认地址 | 是 |
| F05 | 跑腿员 | 资格申请、驳回后重新申请 | 是 |
| F06 | 跑腿员 | 管理员审核并同步用户角色 | 是 |
| F07 | 配置 | 节点启闭和使用中删除保护 | 是 |
| F08 | 配置 | 服务类型启停、价格规则和删除保护 | 是 |
| F09 | 配置 | 服务类型与节点适用规则 | 是 |
| F10 | 任务 | 外卖任务发布及专属明细 | 是 |
| F11 | 任务 | 快递任务发布及专属明细 | 是 |
| F12 | 任务 | 私人跑腿任务发布及专属明细 | 是 |
| F13 | 任务 | 地址归属、服务节点匹配和价格校验 | 是 |
| F14 | 任务 | 待接单任务取消 | 是 |
| F15 | 接单 | 跑腿员资格、空闲状态校验 | 是 |
| F16 | 接单 | 多跑腿员并发抢单控制 | 是 |
| F17 | 派单 | 管理员派单 | 是 |
| F18 | 重派 | 旧跑腿员释放、新跑腿员占用和日志 | 是 |
| F19 | 配送 | `ASSIGNED -> PICKED_UP -> DELIVERING -> WAIT_CONFIRM` 状态机 | 是 |
| F20 | 收货 | 仅发布者确认、重复确认幂等 | 是 |
| F21 | 支付 | 确认收货后支付、稍后付款、重复支付保护 | 是 |
| F22 | 退款 | 申请、管理员审核、支付与任务状态联动 | 是 |
| F23 | 评价 | 完成后一次评价、信誉分调整 | 是 |
| F24 | 投诉 | 发布者投诉、管理员处理、成立后扣分 | 是 |
| F25 | 结算 | 候选筛选、平台费计算、重复结算保护 | 是 |
| F26 | 审计 | 支付/退款/状态日志抽查及明细关联 | 是 |
| F27 | 报表 | 订单/支付/投诉指标和生成记录 | 是 |
| F28 | 安全 | POST Anti-forgery、越权和对象归属检查 | 是 |
| F29 | 架构 | MVC 五层、Service/Repository 接口和依赖注入 | 否 |
| F30 | 配置 | 连接字符串外置和本地覆盖 | 否 |

共整理 30 个功能点，其中 28 个包含业务规则，满足“至少 20 个功能点且至少 15 个有业务逻辑”的工作量口径。这里的“功能点”是测试范围统计，不等同于最终教师认定的软件功能点计量。

## 6. 测试方法

### 6.1 自动化业务测试

通过仓储接口测试替身直接调用 Service，验证输入规范化、状态机、事务提交/回滚、幂等和并发结果。测试不依赖数据库账号，不写入真实业务数据。

并发测试替身使用同一个异步信号量模拟任务行锁：两个跑腿员同时请求同一 `WAITING` 任务，两个请求都进入业务服务，但只有先获得锁的请求能提交；第二个请求获得锁后重新读取状态并回滚。

### 6.2 静态质量门禁

`scripts/run-tests.ps1` 在测试后检查：

- 建表数量不少于 12；当前为 24。
- 所有 `[HttpPost]` Action 均有 `[ValidateAntiForgeryToken]`；当前为 46/46。
- Controller 不引用 Repository 接口。
- Service 中不存在 Oracle Command/Connection 或 `CommandText`。
- Repository 中存在真实的 `FOR UPDATE` 行锁；当前检出 14 处。
- `ConnectionStrings:OracleDb` 在配置文件中声明。

### 6.3 数据库手工/集成测试

在隔离 Oracle 测试库中按真实页面完成主流程，用两个独立会话执行并发抢单。每一步同时检查页面提示和数据库记录，详细步骤及核验 SQL 见 `manual-system-test-guide.md`。

### 6.4 状态机和异常测试

采用合法路径、跳步路径、重复提交、非法枚举、空字段、跨用户访问和不存在 ID 组合验证。数据库用例执行前后记录任务、接派、支付、退款和日志表状态。

## 7. 状态标识

| 状态 | 含义 |
| --- | --- |
| `PASS-AUTO` | 已由本地自动化测试实际执行并通过 |
| `PASS-STATIC` | 已由脚本静态检查实际执行并通过 |
| `PENDING-DB` | 需要隔离 Oracle 测试库，本次未执行，不计入通过率 |
| `KNOWN-FAIL` | 代码审查已确认当前实现不满足预期规则 |
| `KNOWN-GAP` | 当前没有完整实现对应能力，不能执行完整验收 |

## 8. 测试用例表

| 编号 | 功能 | 测试场景 | 输入条件 | 预期结果 | 实际结果 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| TC001 | 注册 | 正常注册 | 新用户名、手机号、合法密码 | 注册成功，密码以哈希保存 | 待隔离库执行 | PENDING-DB |
| TC002 | 注册 | 重复用户名/手机号 | 使用已有账号或手机号 | 友好提示重复，不新增用户 | 待隔离库执行 | PENDING-DB |
| TC003 | 注册 | 空字段和非法格式 | 空用户名、空密码、错误手机号 | 页面校验失败，不写库 | 待隔离库执行 | PENDING-DB |
| TC004 | 登录 | 正确账号密码 | `NORMAL` 状态用户 | 登录成功并获得正确角色 | 待隔离库执行 | PENDING-DB |
| TC005 | 登录 | 错误密码 | 已存在用户、错误密码 | 登录失败，不签发 Cookie | 待隔离库执行 | PENDING-DB |
| TC006 | 登录 | 封禁或注销账号 | `BLOCKED`/`CANCELLED` 用户 | 拒绝登录并显示友好提示 | 待隔离库执行 | PENDING-DB |
| TC007 | 权限 | 未登录访问业务页 | 访问 `/Task`、`/Address` | 跳转登录页 | 待浏览器执行 | PENDING-DB |
| TC008 | 权限 | 普通用户访问管理页 | USER 访问 `/Audit`、`/Report` | 返回拒绝访问，不泄露数据 | 待浏览器执行 | PENDING-DB |
| TC009 | 安全 | POST 防跨站请求伪造 | 扫描所有 POST Action | 每个 POST 均校验 Anti-forgery Token | 46/46 个 POST Action 有防伪属性 | PASS-STATIC |
| TC010 | 地址 | 新增普通地址 | 当前用户、合法联系人和地址 | 新增成功，仅属于当前用户 | 待隔离库执行 | PENDING-DB |
| TC011 | 地址 | 切换默认地址 | 同一用户两条地址 | 仅一条地址为默认 | 待隔离库执行 | PENDING-DB |
| TC012 | 地址 | 跨用户编辑/删除 | 用户 A 提交用户 B 的地址编号 | 拒绝操作，B 的地址不变 | 待隔离库执行 | PENDING-DB |
| TC013 | 地址 | 不存在地址 ID | 不存在的 `address_no` | 返回友好失败或 404，不抛出明细异常 | 待隔离库执行 | PENDING-DB |
| TC014 | 地址 | 同用户并发新增 | 同时提交两个新增请求 | 地址编号不冲突，两条数据均完整 | Service 并发回归测试通过；用户行锁后的真实 Oracle 行为仍待隔离库复核 | PASS-AUTO |
| TC015 | 资格申请 | 普通用户申请跑腿员 | 完整真实姓名和身份信息 | 生成 `PENDING` 申请 | 待隔离库执行 | PENDING-DB |
| TC016 | 资格申请 | 重复提交 | 已有待审核申请再次提交 | 不产生重复有效申请 | 待隔离库执行 | PENDING-DB |
| TC017 | 资格审核 | 管理员通过/驳回 | `PENDING` 申请 | 状态正确变更；通过时用户角色同步为 RUNNER | 待隔离库执行 | PENDING-DB |
| TC018 | 接单资格 | 未审核或非空闲跑腿员抢单 | 非 `APPROVED/FREE` | 抢单失败，任务不变 | 待隔离库执行 | PENDING-DB |
| TC019 | 服务配置 | 停用服务类型发布任务 | `DISABLED` 服务类型 | 页面不可选或 Service 拒绝 | 待隔离库执行 | PENDING-DB |
| TC020 | 节点配置 | 关闭节点发布任务 | `CLOSED` 节点 | 页面不可选或 Service 拒绝 | 待隔离库执行 | PENDING-DB |
| TC021 | 服务节点规则 | 服务和节点不匹配 | 不存在对应规则 | 发布失败并提示“不匹配” | 自动化调用返回业务提示且未成功 | PASS-AUTO |
| TC022 | 任务发布 | 三类任务合法明细 | FOOD/EXPRESS/PRIVATE 完整字段 | 三类模型均通过业务校验 | 3/3 组参数化测试通过 | PASS-AUTO |
| TC023 | 任务发布 | 三类任务缺少专属字段 | 商家、快递单号、取件码、取送地点等为空 | 对具体字段给出校验错误 | 7/7 组缺失字段测试通过 | PASS-AUTO |
| TC024 | 任务发布 | 输入规范化 | 标题/商家前后空格、可选值全空格 | 必填值去空格，可选空白保存为 null | 自动化测试通过 | PASS-AUTO |
| TC025 | 任务发布 | 使用他人地址 | 地址不属于当前发布者 | 拒绝发布，不产生任务及明细 | 待隔离库执行 | PENDING-DB |
| TC026 | 任务取消 | 取消待接单任务 | 发布者、`WAITING` 任务 | 状态变为 `CANCELLED` | 待隔离库执行 | PENDING-DB |
| TC027 | 任务取消 | 已接单后取消 | `ASSIGNED` 或之后状态 | 拒绝并提示只有待接单任务可取消 | 自动化测试验证非法状态提示 | PASS-AUTO |
| TC028 | 抢单并发 | 两个跑腿员同时抢同一任务 | 两个 `APPROVED/FREE` 跑腿员并发请求 | 仅一个成功，仅一条接派记录，任务为 ASSIGNED | 模拟事务行锁的自动化测试通过 | PASS-AUTO |
| TC029 | 抢单并发 | 真实 Oracle 双会话复核 | 隔离库、两个 Cookie/Token、同一任务 | 锁等待后仅一个提交；表中无重复接单 | 未连接隔离 Oracle 执行 | PENDING-DB |
| TC030 | 抢单 | 忙碌跑腿员再次抢单 | 跑腿员 `BUSY` | 抢单失败，不占用第二任务 | 待隔离库执行 | PENDING-DB |
| TC031 | 派单 | 管理员正常派单 | `WAITING` 任务、合格空闲跑腿员 | 任务 ASSIGNED，跑腿员 BUSY，写接派及日志 | 待隔离库执行 | PENDING-DB |
| TC032 | 派单 | 派给不合格跑腿员 | 未审核、离线或忙碌 | 派单失败，所有表保持原状 | 待隔离库执行 | PENDING-DB |
| TC033 | 重派 | 配送中更换跑腿员 | 有效旧接派、新跑腿员 FREE | 旧跑腿员 FREE，新跑腿员 BUSY，新增 REASSIGN 记录 | 待隔离库执行 | PENDING-DB |
| TC034 | 重派 | 重派给原跑腿员 | 新旧 runnerId 相同 | 拒绝重派，不写记录 | 待隔离库执行 | PENDING-DB |
| TC035 | 配送状态 | 按合法顺序流转 | ASSIGNED 依次到 WAIT_CONFIRM | 每步成功并写状态日志 | 3 步状态及日志自动化测试通过 | PASS-AUTO |
| TC036 | 配送状态 | 跳过取货直接配送 | ASSIGNED -> DELIVERING | 拒绝，任务仍为 ASSIGNED，无新日志 | 自动化测试通过 | PASS-AUTO |
| TC037 | 配送权限 | 非当前跑腿员更新状态 | 其他 RUNNER 操作 | 拒绝，状态不变 | 待隔离库执行 | PENDING-DB |
| TC038 | 异常 ID | 更新不存在任务 | 不存在的 taskId | 返回失败，不抛未处理异常 | 待隔离库执行 | PENDING-DB |
| TC039 | 确认收货 | 发布者确认待收货任务 | WAIT_CONFIRM、正确发布者 | 写确认日志，引导付款 | 自动化测试通过 | PASS-AUTO |
| TC040 | 确认收货 | 重复确认 | 同一任务连续提交两次 | 第一次成功，第二次拒绝且无重复日志 | 自动化幂等测试通过 | PASS-AUTO |
| TC041 | 确认收货 | 非发布者确认 | 其他登录用户 | 拒绝，状态和日志不变 | 待隔离库执行 | PENDING-DB |
| TC042 | 支付 | 非法支付方式 | `BANK_CARD` | 事务开始前拒绝，不写支付记录 | 自动化测试通过 | PASS-AUTO |
| TC043 | 支付 | 未确认收货直接支付 | WAIT_CONFIRM 但无确认日志 | 回滚，任务和支付记录不变 | 自动化测试通过 | PASS-AUTO |
| TC044 | 支付 | 确认后立即支付 | 确认日志、合法支付方式 | 支付 PAID，任务 FINISHED，跑腿员 FREE，写日志并提交 | 自动化测试通过 | PASS-AUTO |
| TC045 | 支付 | 重复支付 | 已有 PAID 记录再次提交 | 拒绝，不新增或覆盖已支付记录 | 待隔离库执行 | PENDING-DB |
| TC046 | 支付 | 已退款再次支付 | 已有 REFUNDED 记录 | 拒绝，支付仍为 REFUNDED | 自动化测试通过 | PASS-AUTO |
| TC047 | 支付评价 | 稍后付款后尝试评价 | 任务 FINISHED、支付 UNPAID | 应禁止评价 | 自动化验证 UNPAID/FAILED/REFUNDED 均被拒绝且事务回滚 | PASS-AUTO |
| TC048 | 支付 | 稍后付款再补付 | FINISHED + UNPAID + 已确认 | 原记录更新为 PAID，不产生重复支付 | 待隔离库执行 | PENDING-DB |
| TC049 | 退款 | 已支付任务申请退款 | PAID、FINISHED、有原因 | 退款 APPLY，任务 REFUNDING，写日志 | 待隔离库执行 | PENDING-DB |
| TC050 | 退款 | 重复申请 | 已有 APPLY 或 APPROVED 退款 | 拒绝新增第二条有效退款 | 待隔离库执行 | PENDING-DB |
| TC051 | 退款 | 管理员通过/驳回 | APPLY 退款、合法审核理由 | 审核结果、金额、支付和任务状态一致 | 待隔离库执行 | PENDING-DB |
| TC052 | 评价 | 已支付完成后评价 | PAID、FINISHED、发布者 | 新增一次评价并调整跑腿员信誉分 | 自动化验证评价与信誉分在同一事务提交；真实 Oracle 待复核 | PASS-AUTO |
| TC053 | 评价 | 重复评价 | 同一任务已有评价 | 拒绝，唯一约束不被触发为 500 | 待隔离库执行 | PENDING-DB |
| TC054 | 评价 | 非法评分/过长评论 | 评分不在 1-5 或评论超长 | 友好校验，不写库 | 待隔离库执行 | PENDING-DB |
| TC055 | 投诉 | 发布者投诉已完成服务 | FINISHED、有接派记录 | 生成 SUBMITTED 投诉 | 待隔离库执行 | PENDING-DB |
| TC056 | 投诉 | 跨用户投诉 | 非任务发布者 | 拒绝，不写投诉 | 待隔离库执行 | PENDING-DB |
| TC057 | 投诉 | 重复投诉 | 同一接派记录已有投诉 | 拒绝重复提交 | 待隔离库执行 | PENDING-DB |
| TC058 | 投诉 | 管理员判定投诉成立 | SUBMITTED 投诉、UPHELD | 投诉 DONE，跑腿员信誉分扣 10 | 待隔离库执行 | PENDING-DB |
| TC059 | 结算 | 生成跑腿员结算单 | PAID、FINISHED、无有效退款/未处理投诉、未结算 | 汇总金额、10% 平台费、明细同事务写入 | 待隔离库执行 | PENDING-DB |
| TC060 | 结算 | 重复生成 | 支付已存在于结算明细 | 不重复纳入新结算单 | 待隔离库执行 | PENDING-DB |
| TC061 | 结算 | 排除争议款 | 有待处理投诉或有效退款 | 不进入候选集合 | 待隔离库执行 | PENDING-DB |
| TC062 | 结算状态 | 非法回退/任意跳转 | DONE -> WAITING 等 | 应按明确状态机拒绝非法迁移 | 3 条合法迁移和 3 条非法迁移自动化测试全部通过 | PASS-AUTO |
| TC063 | 审计 | 创建支付/退款/日志审计 | 至少选择一个有效目标 | 主表和对应关联表同事务写入 | 待隔离库执行 | PENDING-DB |
| TC064 | 审计 | 空选择和重复 ID | 空列表或重复 targetId | 空列表拒绝；重复 ID 去重 | 待隔离库执行 | PENDING-DB |
| TC065 | 报表 | 查看统计面板 | 有历史任务、支付、投诉数据 | 指标、节点量和跑腿员绩效可查看 | 待隔离库执行 | PENDING-DB |
| TC066 | 报表 | 生成三类报表记录 | ORDER/PAYMENT/COMPLAINT + 周期 | 计算业务明细并保存 GENERATED 记录 | 有业务数据的生成与事务提交自动化测试通过；三类真实 SQL 待隔离库执行 | PASS-AUTO |
| TC067 | 报表 | 非法类型和非法周期 | 非法枚举、非 `yyyy-MM` 周期 | 友好拒绝，不写库 | 非法周期在事务开始前被拒绝的自动化测试通过 | PASS-AUTO |
| TC068 | 报表 | 报表关联审计依据 | 基于周期内业务和既有审计生成报表 | 写入 report_audit_items | 自动化验证主体与审计关联在同一事务写入 | PASS-AUTO |
| TC069 | 报表 | 导出报表 | GENERATED 报表执行导出 | 产生可下载文件并更新 EXPORTED | 自动化验证 CSV 内容和 EXPORTED 状态更新 | PASS-AUTO |
| TC070 | 异常输入 | 不存在的实体 ID | 各详情、审核、处理 Action 传极大/负数 ID | 404/友好失败，不泄露异常堆栈 | 待隔离库执行 | PENDING-DB |
| TC071 | 异常输入 | 分页越界 | page 为负数，pageSize 为 0 或过大 | 归一到安全范围 | 待隔离库执行 | PENDING-DB |
| TC072 | 架构 | Controller 越层检查 | 扫描 Controller 对 Repository 的引用 | 不存在 Controller 直连 Repository | 静态门禁通过 | PASS-STATIC |
| TC073 | 架构 | Service 持久化泄漏检查 | 扫描 Oracle 类型和 CommandText | Service 不直接写 Oracle 命令 | 静态门禁通过 | PASS-STATIC |
| TC074 | 鲁棒性 | 连接字符串外置 | 检查应用配置 | 存在配置键，支持本地文件/环境变量覆盖 | 静态门禁通过 | PASS-STATIC |
| TC075 | 数据库规模 | 表数量检查 | 扫描结构脚本 | 不少于 12 张表 | 实际 24 张表 | PASS-STATIC |

## 9. 测试结果汇总

### 9.1 已执行结果

| 类型 | 结果 | 说明 |
| --- | --- | --- |
| 自动化测试 | 39/39 通过，0 失败，0 跳过 | 2026-08-22 Release 本地实际执行 |
| 静态质量门禁 | 5 类全部通过 | 24 张表；46/46 POST 防伪；五层边界；14 处行锁；连接配置外置 |
| 共享库只读核验 | 通过 | 24 张表、14 个序列、应用账号权限、`003` 至 `006` 迁移、31 个密码哈希和评价分页查询均正常 |
| 数据库写入型系统测试 | 未完整执行 | 共享库已有任务、支付、退款、结算和导出报表数据，但本轮没有新增评价、投诉或审计数据 |
| 本轮已修复问题 | 5 组 | 评价支付/退款门槛、结算状态机、真实报表与导出、地址事务和并发、封禁实时生效 |
| 仍待验证 | 数据库端到端 | Oracle 真实写事务、并发抢单、评价/投诉/审计页面闭环、结算终态和三类报表 SQL |

自动化测试原始摘要：

```text
已通过! - 失败: 0，通过: 39，已跳过: 0，总计: 39
[PASS] Schema table count: 24 (requirement: >= 12).
[PASS] Anti-forgery coverage: 46/46 POST actions.
[PASS] Layer boundaries: controllers use services; services contain no Oracle commands.
[PASS] Repository row-lock statements found: 14.
[PASS] Oracle connection string is externally configurable via ConnectionStrings:OracleDb.
```

2026-08-15 的原始执行记录保留在 `docs/test-evidence/2026-08-15-local.md`；2026-08-22 结果由同一脚本重新执行取得。

### 9.2 阶段性结论

当前项目已经具备“针对核心业务流程、异常情况和并发场景进行了验证”的可审查证据，且测试工程保持小而有价值，没有为凑数量引入大量 CRUD 测试。

`TC047`、`TC062` 和报表完整性缺口已修复并有自动化回归证据，数据库迁移阻断也已解除；但在完成剩余 `PENDING-DB` 写入型用例前，仍不应对外声称“所有数据库端到端用例全部通过”。

## 10. 已修复问题与剩余验证

| 编号 | 原级别 | 原问题 | 修复结果 | 剩余验证 |
| --- | --- | --- | --- | --- |
| ISSUE-01 | 高 | 未付款或退款中任务可能评价 | 已修复：稍后付款不再提前置 FINISHED；评价事务同时要求 FINISHED、PAID、无活动退款 | 隔离 Oracle 执行评价/退款竞争用例 |
| ISSUE-02 | 中 | 结算状态可任意切换 | 已修复：事务内锁定结算单并执行明确迁移表，DONE 为终态 | 隔离 Oracle 复核并发更新 |
| ISSUE-03 | 中 | 报表只有元数据，无审计关联和导出 | 已修复：按月查询业务数据、生成指标/明细、关联审计、CSV 导出并更新 EXPORTED | 共享库已有 1 条 EXPORTED 订单报表；投诉、绩效报表查询和不可变快照策略仍待完整验收 |
| ISSUE-04 | 中 | 地址 `MAX+1` 和默认切换缺少并发/事务保护 | 已修复：先锁用户行再分配编号；默认切换、删除补位均在同一事务 | 单默认地址唯一索引已验证；隔离 Oracle 双会话写入复核仍待执行 |
| ISSUE-05 | 中 | 封禁后旧 Cookie 继续有效 | 已修复：Cookie `OnValidatePrincipal` 每个已认证请求重新读取账号状态和角色 | 浏览器连接隔离库验证封禁后的下一次请求被退出 |

## 11. 回归准入标准

后续提交至少满足：

1. `scripts/run-tests.ps1` 全部通过。
2. 不减少 46/46 POST 防伪覆盖。
3. 抢单、支付或退款变更必须补充事务/状态回归测试。
4. 数据库结构变化后更新 24 表统计、数据库字典和测试 SQL。
5. 评价支付/退款门槛、地址事务、结算状态机和报表生成/导出变更必须保留对应回归测试。
6. 答辩前在隔离库完成所有主链路和 `TC029`，将结果、截图或 SQL 输出归档到 `docs/test-evidence/manual/`。
