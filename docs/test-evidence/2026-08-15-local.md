# 2026-08-15 本地自动化测试证据

## 执行信息

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-08-15 |
| 执行目录 | 仓库根目录 |
| 目标框架 | .NET 9.0 |
| 测试运行器 | VSTest 17.14.1 x64 |
| 测试框架 | xUnit 2.9.2 |
| 数据库连接 | 未连接；自动化测试使用仓储接口测试替身 |

## 执行命令

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-tests.ps1
```

## 原始结果摘要

```text
[TEST] Running focused business-service tests (Debug)...
CampusDelivery.Api -> backend\src\CampusDelivery.Api\bin\Debug\net9.0\CampusDelivery.Api.dll
CampusDelivery.Tests -> backend\tests\CampusDelivery.Tests\bin\Debug\net9.0\CampusDelivery.Tests.dll

已通过! - 失败: 0，通过: 21，已跳过: 0，总计: 21
[PASS] Schema table count: 24 (requirement: >= 12).
[PASS] Anti-forgery coverage: 45/45 POST actions.
[PASS] Layer boundaries: controllers use services; services contain no Oracle commands.
[PASS] Repository row-lock statements found: 11.
[PASS] Oracle connection string is externally configurable via ConnectionStrings:OracleDb.
```

## 自动化覆盖明细

| 测试类 | 数量 | 主要证据 |
| --- | ---: | --- |
| `AssignServiceTests` | 4 | 双跑腿员竞争仅一个成功、非法状态跳转、合法配送链、重复确认收货 |
| `TaskServiceTests` | 13 | 三类任务 7 个专属必填字段、3 类合法模型、服务节点规则、输入规范化、非法取消 |
| `PaymentServiceTests` | 4 | 非法支付方式、未确认收货回滚、成功支付事务、退款后禁止再次支付 |
| 合计 | 21 | 21 通过，0 失败，0 跳过 |

## 证据边界

- 抢单测试证明 `AssignService` 在事务和任务行锁语义下只允许一个请求提交，并验证只生成一条接派记录和状态日志。
- 静态检查证明真实 `AssignRepository` 使用 `SELECT ... FOR UPDATE`，并且项目存在统一提交/回滚事务管理器。
- 本记录不证明真实 Oracle 双会话已经执行。数据库锁等待、最终表行数和会话隔离证据须按 `manual-system-test-guide.md` 的 `TC029` 在隔离测试库中补录。
- 本次没有使用或修改云端数据库数据，也没有把待执行用例标记为 PASS。

## 复现位置

```text
backend/tests/CampusDelivery.Tests/AssignServiceTests.cs
backend/tests/CampusDelivery.Tests/TaskServiceTests.cs
backend/tests/CampusDelivery.Tests/PaymentServiceTests.cs
backend/tests/CampusDelivery.Tests/TestDoubles.cs
scripts/run-tests.ps1
```
