# 验收环境记录

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-08-16 |
| Git 分支 | `test` |
| Git 提交 | `2917db9` |
| 本地应用 | `http://127.0.0.1:5280/` |
| 云端应用 | `http://47.116.60.57:5227/`，当前仍为旧版 |
| 数据库目标 | Oracle 19c / `orclpdb1` |
| 自动化测试 | 39/39 通过 |
| 页面截图环境 | 本地新版，公开页面无需数据库 |
| 数据库连接 | SSH 隧道已恢复；应用页面显示 Oracle 已连接，APPUSER 用户数 28 |
| 数据库系统测试 | 仅执行只读核验；未执行注册、发布、审核、生成报表等写操作 |

## 已验证事实

- 新版首页、登录页和注册页可以正常渲染。
- 需要认证的新版路由会正确跳转登录页。
- 管理员已有登录会话可正常读取配置、退款、结算、审计和报表页面。
- 密码迁移工具 `--dry-run` 只读结果为 `TOTAL=28`、`IDENTITY=28`、`LEGACY_CANDIDATE=0`、`UNSUPPORTED=0`。
- `/Review/All` 当前进入统一错误页；只读结构查询确认 `APPUSER.reviews` 缺少新版要求的 `task_id`，已登记 `BUG-20260816-03`。
- `004` 和 `006` 的关键约束/索引均未在 APPUSER 检出：`UK_REVIEWS_TASK`、`FK_REVIEWS_RECORD_TASK`、`UK_USER_ADDRESSES_ONE_DEFAULT`、`UK_SERVICE_TYPES_NAME_CI` 计数均为 0，详见 `sql/ENV-07-migration-status.txt`。
- 云端新路由仍返回 404，不能作为新版功能验收环境。

## 证据边界

ENV-01 至 ENV-03 只证明页面存在、布局可用和入口真实。ENV-04 及 FLOW-05、16 至 20 使用现有管理员会话和只读 SQL 采集；`PASS-READONLY` 只证明当前页面与已有数据库结果一致，不代表本次重新执行了写入流程。其余核心业务流程仍须在获准的隔离测试数据中执行，不能将待补拍项提前标记为 PASS。
