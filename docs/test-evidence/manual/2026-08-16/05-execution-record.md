# 本轮截图执行记录

## 执行信息

| 项目 | 内容 |
| --- | --- |
| 执行时间 | 2026-08-16 11:43:54 +08:00 |
| Git 分支 | `test` |
| Git 提交 | `2917db9ba55a9385f81e5f16242dd454c796ad76` |
| 应用地址 | `http://127.0.0.1:5280/` |
| 数据库 | Oracle 19c，经本地 SSH 隧道连接 |
| 浏览器证据 | 1265 × 712 PNG，页面缩放保持默认 |
| 数据库操作 | 仅 SELECT；未执行 INSERT、UPDATE、DELETE、DDL 或页面写入表单 |

## 已执行项目

1. 复核公开首页、登录页、注册页和管理员工作台。
2. 访问 `/Database/Status`，页面显示连接成功、用户数量 28。
3. 只读查看节点、服务类型和服务节点规则，并用 SQL 对照编号、状态及绑定关系。
4. 只读查看已有退款、结算单和结算支付明细，并用 SQL 对照状态与金额。
5. 查看审计和报表页面；数据库证明确有 21 个任务、5 条支付、3 条退款，但审计记录与报表生成记录均为 0。
6. 使用无 Cookie 的 `localhost` 会话访问 `/Report`，确认被重定向至 `/Auth/Login?ReturnUrl=%2FReport`。
7. 访问 `/Review/All`，稳定出现统一错误页，登记 `BUG-20260816-03`。
8. 访问支付审计创建页，读取到 5 条候选支付；页面标题错误显示 Razor 表达式，登记 `BUG-20260816-04`。
9. 重新运行 `scripts/run-tests.ps1`：39/39 通过，5 类静态门禁全部通过；日志及截图归档为 ENV-05。
10. 只读核对 APPUSER 表结构，确认 `reviews.task_id` 和 `004`/`006` 关键约束、索引尚未落库，登记 `BUG-20260816-05`。

## 未执行及原因

- 注册、地址维护、资格申请/审核、发布任务、抢单、派单、配送、确认收货和支付等会写数据库的流程未执行。
- 投诉、审计和报表当前没有可供完整展示的已生成记录。
- 目标数据库为团队共享环境；在负责人确认测试数据范围或提供隔离库前，不应为截图擅自写入。
- APPUSER 当前数据库结构落后于提交 `2917db9`；应先由数据库负责人备份并执行迁移预检查，再继续写入型验收。
- 云端仍是旧版本，不能替代本地新版作为答辩证据。

## 结论

本轮补齐了所有当前可安全取得的真实只读证据，并修正了错误截图命名。证据包仍不是最终完整 PASS；完成条件是获得共享库写入授权或隔离测试库后，按 `02-capture-checklist.md` 补拍所有 `PENDING-ENV`/`PARTIAL` 项并同步回填 SQL 结果。
