# 2026-08-16 截图证据包

## 1. 证据包状态

本目录用于保存提交 `2917db9` 的功能验收截图和数据库只读核验结果。

- 当前已完成：公开页面、登录/注册页面、管理员工作台、数据库连接、配置、退款、结算、审计、报表和未登录权限控制的截图；对应只读 SQL 已归档。
- 当前待补拍：用户/跑腿员写入主流程、并发抢单、投诉处理、审计生成、报表生成与导出，以及负向重复提交场景。
- 当前限制：Oracle SSH 隧道已恢复，但目标是团队共享数据库；在未确认可写测试数据范围前，本轮只执行 SELECT 和页面 GET。只读结构检查还确认 `004`/`006` 关键迁移未落库。云端 `http://47.116.60.57:5227/` 仍运行旧版本。
- 迁移证据说明：`sql/MIGRATION-precheck.txt` 是 SQL*Plus 未建立连接时产生的失败原始日志，不能作为预检查结论；有效的只读迁移状态以 `sql/ENV-07-migration-status.txt` 为准。
- 证据原则：不使用静态原型、伪造数据或手工修改数据库状态冒充通过结果。

## 2. 目录结构

```text
screenshots/              实际页面截图
sql/                      SQL Developer 只读核验脚本和输出
logs/                     自动化测试原始结果摘要
01-screenshot-index.md    截图索引与验收结论
02-capture-checklist.md   补拍步骤、账号和文件名
03-environment.md         环境、提交与执行边界
04-bug-record.md          截图过程中发现的问题
05-execution-record.md    本轮执行时间、范围和证据边界
```

## 3. 完成标准

证据包只有在 `01-screenshot-index.md` 中所有核心流程均标记为 `PASS`，且每个涉及数据变化的流程同时具备页面截图和 SQL 查询结果时，才可对外称为“完整”。`PASS-READONLY` 和 `PARTIAL` 均不等同于本次端到端执行通过。

每张截图必须满足：

1. 文件名与索引编号一致。
2. 页面中能看到功能名称、关键状态或成功提示。
3. 隐藏密码、连接串、手机号、身份证号等敏感信息。
4. 涉及写操作时，附对应 SQL Developer 查询结果。
5. 失败截图必须登记缺陷编号，不能作为通过证据。
