# 组员4基础资料与配送员测试执行记录

## 1. 执行信息

| 项目 | 内容 |
| --- | --- |
| 测试范围 | `TC-BASE-01` 至 `TC-BASE-08` |
| 执行标识 | `B40825142756` |
| 执行时间 | 2026-08-25 |
| 分支 | `test` |
| 基线提交 | `18a00c6741ac71f8d9b8cf42a6e068b78b1db1b3`，另含本轮“个人中心显示跑腿员姓名”工作区修改 |
| 应用地址 | `http://127.0.0.1:5227` |
| 数据库 | 共享 Oracle 19c，经 `127.0.0.1:15210/orclpdb1` SSH 隧道连接 |
| 自动化门禁 | Release `47/47` 通过；24 表、防伪令牌、五层边界、行锁和连接配置检查通过 |
| 数据库状态页 | `Oracle connected successfully` |

测试计划仍写“39 项自动化测试”，当前提交实际为 47 项。本记录采用本次真实执行结果，不将计划中的旧数量写成现状。

本轮通过注册页面创建三个隔离账号。管理员夹具仅通过数据库将本轮专用账号角色提升为 `ADMIN`；节点、服务类型、绑定、任务和跑腿员申请/审核等被测业务均通过 MVC 页面执行。未记录或提交任何密码、Cookie、SSH 私钥或数据库连接凭据。

## 2. 结果汇总

| 用例编号 | 实际结果 | 结果 | 证据 |
| --- | --- | --- | --- |
| TC-BASE-01 | 新增节点 `#101`，修改名称和位置后依次切换为 `CLOSED`、`NORMAL`；关闭时发布页不再提供节点 `#101` | 通过 | `screenshots/TC-BASE-01-1-节点新增.png` 至 `TC-BASE-01-5-关闭节点发布页不可选.png`；`sql/TC-BASE-查询.sql` |
| TC-BASE-02 | 任务 `#255` 引用节点 `#101` 后尝试删除，页面提示已有任务记录不能删除；节点仍存在且历史任务数为 1 | 通过 | `screenshots/TC-BASE-02-使用中节点删除保护.png`；SQL 查询结果 |
| TC-BASE-03 | 新增服务类型 `#101`，状态由 `ENABLED` 切换到 `DISABLED` 后发布页不再提供，再恢复为 `ENABLED` | 通过 | `screenshots/TC-BASE-03-1-服务类型新增.png` 至 `TC-BASE-03-3-停用服务发布页不可选.png`；SQL 查询结果 |
| TC-BASE-04 | 服务 `#101` 与节点 `#101` 绑定后发布页可选；解除后篡改提交被服务端拒绝，标题为 `B40825142756未绑定组合` 的任务落库数为 0；最后重新绑定 | 通过 | `screenshots/TC-BASE-04-1-服务节点绑定.png` 至 `TC-BASE-04-4-解绑后发布拒绝.png`；SQL 查询结果 |
| TC-BASE-05 | 用户 `b4runA8076859` 提交申请，生成 runner `#381`，申请后为 `PENDING / OFFLINE` | 通过 | `screenshots/TC-BASE-05-跑腿员申请.png`；SQL 查询结果 |
| TC-BASE-06 | 管理员审核 runner `#381` 后，资格为 `APPROVED`、工作状态为 `FREE`，用户角色同步为 `RUNNER` | 通过 | `screenshots/TC-BASE-06-管理员审核通过.png`；SQL 查询结果 |
| TC-BASE-07 | runner `#382` 经历 `PENDING → REJECTED → PENDING → APPROVED`，驳回时保持 `USER / OFFLINE`，复审通过后为 `RUNNER / FREE` | 通过 | `screenshots/TC-BASE-07-1-申请被驳回.png` 至 `TC-BASE-07-3-重申后审核通过.png`；SQL 查询结果 |
| TC-BASE-08 | runner `#381` 最终为 `APPROVED / FREE`，账号为 `RUNNER / NORMAL`，可访问 `/Task/Hall` 并显示接单入口 | 通过 | `screenshots/TC-BASE-08-可接单账号任务大厅.png`；SQL 查询结果 |

结论：本轮 `TC-BASE` 共 8 条，8 条通过，0 条失败，0 条阻塞。

## 3. 补充验证：个人中心显示跑腿员姓名

用户反馈跑腿员个人中心只显示登录账号，而管理员派单页面可以显示 `runners.real_name`。核查确认个人资料原先只读取 `users`，没有把跑腿员姓名传入页面。

本轮补充实现并验证：

- runner `#381` 打开 `/User/Profile` 后，页面顶部显示真实姓名“甲测试员”，同时保留登录账号 `b4runA8076859`。
- 账户信息区域新增“跑腿员姓名：甲测试员”。
- 非跑腿员账号仍按原方式显示账号，不出现空的跑腿员姓名字段。
- 五项账户资料使用两列布局时，末项“账户状态”横跨两列，姓名与联系电话下方的分隔线连续一致。
- 修改后的 Release 自动化门禁为 `47/47` 通过，静态门禁全部通过。

证据：`screenshots/补充-跑腿员个人中心显示姓名.png`、`screenshots/补充-账户信息分隔线修复.png`。

## 4. 最终保留状态

- 节点 `#101`：`NORMAL`，名称 `B40825142756测试节点-已修改`。
- 服务类型 `#101`：`ENABLED`，基础价格 `9.99`。
- 服务节点规则：服务 `#101` 与节点 `#101` 已绑定。
- 历史引用任务 `#255`：`WAITING`，供删除保护复核。
- runner `#381`、`#382`：均为 `APPROVED / FREE`。

这些均为带执行标识的专用测试记录，未清理团队既有数据。

## 5. 证据边界

本记录证明当前提交、当前共享库和本轮浏览器环境下的 TC-BASE 行为。它不外推为压力容量、浏览器兼容矩阵、生产安全或其他模块全部通过。
