查询：查看差评及信誉变动
SELECT review_id, task_id, rating, credit_delta, reviewed_at
FROM reviews
WHERE task_id = &tid;
结果：

查询：查看扣减后的信誉分（应不低于 0）
SELECT runner_id, real_name, credit_score
FROM runners
WHERE runner_id = &runner;
结果：

