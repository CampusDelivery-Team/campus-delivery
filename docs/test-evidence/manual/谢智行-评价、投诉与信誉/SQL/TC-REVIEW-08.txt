查询：处理前查看投诉状态
SELECT complaint_id, record_id, reason, process_status, process_result
FROM complaints
WHERE complaint_id = &cid;
结果：

查询：处理后确认状态与处理结果
SELECT complaint_id, record_id, reason, process_status, process_result
FROM complaints
WHERE complaint_id = &cid;
结果：

